data/*
!data/.gitkeep
tests/browser/node_modules/
tests/browser/test-results/
tests/browser/playwright-report/
*.log
.env
# Internal coding-agent guardrails — local-only, not for public consumption.
# Strip the homelab-specific deployment context (URLs, RAM tuning, wrapper
# internals) before any external publish.
AGENTS.md
# docs/ is local planning material; keep the whole tree out of git.
docs/
tests/hermes_test/
docs/todo_enc_keys.md
